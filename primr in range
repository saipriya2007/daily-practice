#include<iostream>
using namespace std;

int main()
{
    int f;
    int l;
    cout<<"enter from where"<<endl;
    cin>>f;
    cout<<"till where"<<endl;
    cin>>l;



    for(int j = f; j<=l; j++)
    {
        int cnt = 0;
        for(int i = 1; i<=j; i++)
        {
            if( j%i == 0)
            {
                cnt++;
            }
        }
        if(cnt == 2)
        {
            cout<<j;
        }
    }

    cout<<endl;
    system("pause");
    return 0;
}
