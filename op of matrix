#include <iostream>

using namespace std;

int main()
{
    int m;
    int n;


    int matA[m][n];

    for(int i=1;i<=m;i++)
    {
        for(int j=1;j<=n;j++)
        {
            matA[i][j];
        }
    }

    cout<<"the answer is "<<endl;

    for(int i=1;i<=m;i++)
    {
        for(int j=1;j<=n;j++)
        {
            cout<<matA[i][j];
        }
    }

    cout<<endl;
    system("pause");
    return 0;
}
